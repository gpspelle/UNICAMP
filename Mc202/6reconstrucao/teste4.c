#include <stdio.h>

typedef char string[1000];
int main(void) { 

	string pre_or;
	string em_or;
	
	while(scanf("%s %s", pre_or, em_or)==2) {
		printf("Lido: >>%s<< e >>%s<<\n", pre_or, em_or);
	
		
	}
	return 0;
}
